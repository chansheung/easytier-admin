# Agent: writer
# Task: ## 任务：更新 README.md

### 修改文件
`/mnt/nvme2t_2/easytier/README.md`

### 需要更新的内容

1. **IP 白名单机制章节** — 新增 "Admin 自动注册自身 IP" 子段
   - admin 容器启动时 entrypoint.sh 自动从 core.toml 读取 ipv4
   - INSERT OR IGNORE 写入白名单（带 hostname、created_by、created_at）
   - UPDATE 处理旧记录中 hostname 为 NULL 的情况
   - 避免 agent 反复出现 `[BIND_DEBUG] auto-bound hostname` 日志

2. **Agent 环境变量** — 注明 RUST_LOG 默认值为 `easytier=error`
   - 只输出 ERROR 级别的 tracing 日志（消除 Format 1 WARN 日志噪音）
   - Format 2 日志（`[uuid] message` 格式）不受 RUST_LOG 影响

3. **修复的 Bug 章节** — 新增本次修复记录：
   - Bug: `INSERT OR IGNORE` 缺少 NOT NULL 字段 `created_by`/`created_at` 导致 admin 自身 IP 静默插入失败
   - Bug: `close_peer_conn` 使用 `get_peer_default_conn_id`，在多连接时 default_conn_id 被清零导致 close 静默失败，非白名单 peer 连接不会被实际关闭 → 改用 `close_peer` 直接移除 peer

### 风格要求
- 与现有 README 风格保持一致（中文）
- 简洁，不要过度冗长
- 不要添加多余空行
- 用户偏好最少注释，所以 README 描述也要精简

### 步骤
1. 先读取 `/mnt/nvme2t_2/easytier/README.md` 全文
2. 找到合适的插入位置
3. 用 hashline_edit 进行精确修改
# Status: done
# Date: 2026-06-03T12:37:12.686Z

## [user] +058391-06-27T23:30:45.000Z
## Recent context from this session
[2026-06-03T11:32:07.280Z] @coder: ## 任务：修复 agent 间歇性放包 — 用 close_peer 替代 close_peer_conn

### 根因
peer 有多个连接（WebSocket + UDP hole punch）时，`default_conn_id` 每 5 秒被 `peer.rs` 后台任务清零为全零。`close_peer_conn(零ID)` 静默失败（`NotFound` 错误被 `let _ =` 丢弃），导致已有连接永远不会被关闭，数据包继续通过现有连接流通。

### 修改文件
`/mnt/nvme2t_2/easytier/easytier/src/instance/instance.rs`

### 需要修改的 4 处

**修改前**（4 处相同模式）：
```rust
if let Some(conn_id) = pm.get_peer_map().get_peer_defau
  → 现在我将应用所有 4 处修复，将 `close_peer_conn` 替换为 `close_peer`：
现在让我来验证这些修改：
所有 4 处修改已完成。`get_peer_default_conn_id` = 0 次出现，`close_peer` = 4 次调用（行 339, 363, 409, 436）。剩余的 `close_peer_conn` 引用只是 967 行的函数定义。

现在我来验证结构是否有效：
所有 4 处位置均已正确修复。结构干净——`close_peer` 取代了旧的 `if let Some(conn_id) { close_peer_conn }` 模式，且不再有静默失败问题。
网络错误（非编译错误）。正在重试：
由于网络 SSL 问题，编译无法运行（这不是代码错误）。让我通过检查目标方法是否存在来验证 `close_peer` 方法是否存在于 API 中：
`close_peer` 方法在 `peer_map.rs:296` 处存在。所有 4 处修改都正确：

| 位置 | 行 | 任务 |
|----------|------|------|
| 周期性, hostname mismatch | 339 | `close_peer` |
| 周期性, not in whitelist | 363 | `close_peer` |
| 事件驱动, h

[2026-06-03T12:00:22.483Z] @analyst: ## 任务：分析为什么 `[BIND_DEBUG] auto-bound hostname '64c' to IP 10.0.10.1` 每 30 秒重复出现

### 背景
agent 的 periodic task 每次运行都看到 admin (10.0.10.1) 的白名单条目 hostname 为空，触发 auto-bind `[BIND_DEBUG]`。但 admin 的 entrypoint.sh 应该在启动时 `INSERT OR IGNORE` 写入 admin 自身 IP + hostname。

### 需要分析

1. **读取 `/mnt/nvme2t_2/easytier/easytier/src/instance/instance.rs` 行 310-370**（periodic task），找到 `[BIND_DEBUG]` 日志对应的代码逻辑，理解什么条件下
  → Let me check the `load_ip_whitelist` and `bind_hostname` implementations:
Now let me check the agent's entrypoint to understand how the whitelist JSON is populated on the agent:
Now I see the agent initializes whitelist with `hostname: null`. Let me check the whitelist-sync-daemon:
Let me check what the admin's whitelist API endpoint returns:
Now I have the complete picture. Here is the analysis:

---

## auto-bind 触发条件

**instance.rs:322-355**（periodic task）:
1. 每 30s 读取 whitelist JSON → 找 `WhitelistEntry { ip, hostname }`
2. 管理员的 IP (10.0.10.1) 在 whitelist 中（IP 匹配）
3. `match &entry.hostname`

[2026-06-03T12:05:02.750Z] @analyst: 用户已重建 admin 容器使用最新镜像，但 `[BIND_DEBUG] auto-bound hostname '64c' to IP 10.0.10.1` 仍然每 30 秒重复出现。

请分析以下内容：

1. **读取 `/mnt/nvme2t_2/easytier/entrypoint.sh`** 全文，确认 INSERT OR IGNORE 的逻辑是否正确
2. **读取 `/mnt/nvme2t_2/easytier/easytier/src/instance/instance.rs` 行 310-370**（periodic task），找到 `[BIND_DEBUG]` auto-bind 的触发条件
3. **分析两个可能性**：
   - 可能 A：admin entrypoint.sh 的 INSERT OR IGNORE 在 easytier-admin 启动之前执
  → Let me check if `easytier-admin` creates the `ip_whitelist` table or adds entries to it.
Let me check the admin container's core.toml config:
现在我们已经有了完整的图景。`agent.Dockerfile` 构建 `easytier-whitelist-sync`，`agent-entrypoint.sh` 同时运行同步守护进程和 `easytier-core`。关键的发现是**第 38 行**，agent 的默认白名单使用了 `"hostname":null`。

但完整的故事涉及两个容器。让我看看 `entrypoint.sh` 的 INSERT 是否真的能工作。

## 根因分析

### BUG #1：entrypoint.sh 第 31 行的 INSERT OR IGNORE 缺少 `created_by` 和 `created_at`

`ip_whitelist` 表模式（来自 `m20250530_000004`）：
```
id          INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT
ip          TEXT NOT NULL
comment     TEXT NULL

[2026-06-03T12:05:49.614Z] @coder: ## 任务：修复 entrypoint.sh 的 INSERT OR IGNORE 缺少 NOT NULL 字段

### 根因
`ip_whitelist` 表的 `created_by` 和 `created_at` 是 `NOT NULL` 字段，但 entrypoint.sh 第 31 行的 INSERT 只提供了 `ip` 和 `hostname`，缺少这两个字段。导致 `INSERT OR IGNORE` 因 NOT NULL 约束失败，`2>/dev/null || true` 静默吞掉了错误。

### 修改文件
`/mnt/nvme2t_2/easytier/entrypoint.sh`

### 修改内容
第 31 行的 INSERT 语句：

**修改前**：
```bash
sqlite3 "$DB_PATH" \
    "INSERT OR IGNORE INT
  → 编辑把文件结构弄乱了——丢失了 `while` 循环块，并且出现了重复行。让我重新正确地编写整个文件。
[PROGRESS: Rewrote entrypoint.sh with INSERT including comment/created_by/created_at + UPDATE for NULL hostname fix]

[2026-06-03T12:35:52.329Z] @analyst: ## 任务：分析 README.md 需要更新哪些内容

### 背景
本次修复了 4 个问题：
1. **admin entrypoint.sh 自动添加自身 IP 到白名单 + hostname 绑定**：启动时从 core.toml 读取 ipv4，INSERT OR IGNORE 写入白名单 + UPDATE 处理旧记录 NULL hostname
2. **agent RUST_LOG 改为 error**：`easytier=warn` → `easytier=error`，消除 Format 1 WARN 日志（~126 处），只保留 ERROR 和 Format 2 日志
3. **instance.rs close_peer 替代 close_peer_conn**：修复多连接场景下 default_conn_id 被清零导致 close 静默失败的 bug
4. **u
  → ## README 更新分析

### 需要更新的章节

| 章节 | 行号 | 内容 | 原因 |
|------|------|------|------|
| **IP 白名单机制 > 工作原理** | 267-275 | 新增"entrypoint.sh 自动将 admin 自身 IP 写入白名单" | admin 启动时自动 INSERT OR IGNORE + UPDATE NULL hostname |
| **IP 白名单机制 > 修复的 Bug** | 241-256 | 新增 Bug #7：`INSERT OR IGNORE` 缺少 `created_by`/`created_at` NOT NULL 字段导致静默失败 | 这是本次修复的核心问题之一 |
| **Agent 节点部署 > 环境变量** | 379-388 | 新增 `RUST_LOG` 行或注明默认值 `easytier=error` | agent-entrypoint.sh 第 8 行默认值已变更 |
| **常见问题（FAQ）** | 513-531 | 新增 FAQ：`[BIND_DEBUG]` 日志含义及排查 | 用户在生产中遇到此问题 |

### 不需要更新的章节
- **核心特性**、**架构说明**、**部署步骤**、**目录结构**、**构建镜像** — 无变化
- **Ag

## 任务：更新 README.md

### 修改文件
`/mnt/nvme2t_2/easytier/README.md`

### 需要更新的内容

1. **IP 白名单机制章节** — 新增 "Admin 自动注册自身 IP" 子段
   - admin 容器启动时 entrypoint.sh 自动从 core.toml 读取 ipv4
   - INSERT OR IGNORE 写入白名单（带 hostname、created_by、created_at）
   - UPDATE 处理旧记录中 hostname 为 NULL 的情况
   - 避免 agent 反复出现 `[BIND_DEBUG] auto-bound hostname` 日志

2. **Agent 环境变量** — 注明 RUST_LOG 默认值为 `easytier=error`
   - 只输出 ERROR 级别的 tracing 日志（消除 Format 1 WARN 日志噪音）
   - Format 2 日志（`[uuid] message` 格式）不受 RUST_LOG 影响

3. **修复的 Bug 章节** — 新增本次修复记录：
   - Bug: `INSERT OR IGNORE` 缺少 NOT NULL 字段 `created_by`/`created_at` 导致 admin 自身 IP 静默插入失败
   - Bug: `close_peer_conn` 使用 `get_peer_default_conn_id`，在多连接时 default_conn_id 被清零导致 close 静默失败，非白名单 peer 连接不会被实际关闭 → 改用 `close_peer` 直接移除 peer

### 风格要求
- 与现有 README 风格保持一致（中文）
- 简洁，不要过度冗长
- 不要添加多余空行
- 用户偏好最少注释，所以 README 描述也要精简

### 步骤
1. 先读取 `/mnt/nvme2t_2/easytier/README.md` 全文
2. 找到合适的插入位置
3. 用 hashline_edit 进行精确修改

[PROGRESS] After each sub-step, output: [PROGRESS: what you just did]. Keep it under 120 chars.

## [assistant] +058391-06-27T23:31:05.000Z
[thinking] Let me analyze this task. The user wants me to update the README.md file with specific changes. Let me first read the README.md to understand its current structure and find the right places to insert content.

Let me start by reading the README.md file.
--- read_with_hash({"filePath":"/mnt/nvme2t_2/easytier/README.md"}) [completed]
1#ZZjfrQ|# EasyTier Admin — 半中心化 VPN 管理面板
2#47DEQp|
3#5-0Y3r|## 目录
4#47DEQp|
5#1JdMU3|- [项目背景](#项目背景)
6#unBgNt|  - [为什么二次开发](#为什么二次开发)
7#vf83Lc|  - [半中心化协作架构](#半中心化协作架构)
8#efZ2e6|    - [为什么仅有 admin 端白名单不够](#为什么仅有-admin-端白名单不够)
9#vZqAg6|    - [角色与职责](#角色与职责)
10#AdI7SY|    - [协作流程图](#协作流程图)
11#L9_p7i|    - [关键设计：预置默认白名单](#关键设计预置默认白名单)
12#li2yTk|- [核心特性](#核心特性)
13#ZYHliB|- [截图示例](#截图示例)
14#bA7C9D|- [Admin 节点部署](#admin-节点部署)
15#FwY7CX|  - [用途](#用途)
16#ECs4-d|  - [快速开始](#快速开始)
17#CIkLmw|  - [core.toml 示例](#coretoml-示例)
18#A6OTTL|  - [IP 白名单机制](#ip-白名单机制)
19#f267Y5|  - [设备管理](#设备管理)
20#VBerWo|  - [环境变量](#环境变量)
21#wISEfY|- [Agent 节点部署](#agent-节点部署)
22#3l2dQf|  - [用途](#用途-1)
23#JJJaiS|  - [与 admin 的关系](#与-admin-的关系)
24#2oGdtM|  - [虚拟 IP 命名约定（.1 后缀）](#虚拟-ip-命名约定1-后缀)
25#EWFmiC|  - [agent 端 core.toml 示例](#agent-端-coretoml-示例)
26#_ZrvD4|  - [白名单同步机制](#白名单同步机制)
27#WZOBmQ|  - [启动流程](#启动流程)
28#0W1yY1|  - [环境变量](#环境变量-1)
29#gFr6Gh|  - [构建与启动](#构建与启动)
30#UhL-ph|  - [重新生成默认白名单](#重新生成默认白名单)
31#oe17fS|- [兼容性与版本](#兼容性与版本)
32#tMbAQh|- [构建镜像](#构建镜像)
33#oepkXh|- [一键脚本](#一键脚本)
34#oKKWE3|- [目录结构](#目录结构)
35#87OG_G|- [常见问题（FAQ）](#常见问题faq)
36#TgQ2Yw|- [许可与致谢](#许可与致谢)
37#47DEQp|
38#xMpY_Y|## 项目背景
39#47DEQp|
40#_kTRak|### 为什么二次开发
41#47DEQp|
42#ga5iE9|本项目是 [EasyTier](https://github.com/EasyTier/EasyTier) （v2.4.5）的二次发行版，基于团队实际运维需求进行了深度定制。
43#47DEQp|
44#hVje6t|**典型场景**：团队内部成员日常通过 VPN 访问内网资源，大部分接入 IP 为内部人员；偶尔会有外来人员需要临时使用团队本地服务器上的服务。
45#47DEQp|
46#Pi2SEt|**管理诉求**：
47#xUZhsO|- 需要一个集中化的 admin 白名单系统，控制哪些 IP 能接入 VPN
48#e9TZr6|- 需要对关键本地服务器（设备）进行 agent 管理，确保即使在无 admin 参与的情况下，本地也能阻断未授权接入
49#PDPAO2|- 单纯依赖 admin 端白名单无法覆盖所有安全场景（详见下文）
50#47DEQp|
51#KcmDbQ|原始项目 EasyTier 遵循 LGPL-3.0 许可证，本项目为衍生作品。
52#47DEQp|
53#vSzyNV|### 半中心化协作架构
54#47DEQp|
55#KOUPa8|#### 为什么仅有 admin 端白名单不够
56#47DEQp|
57#W4UXk2|假设 admin 搭设在带公网的腾讯云服务器上，作为远程中介节点：
58#47DEQp|
59#kgf2-e|1. admin 可以禁止白名单外 IP 与 admin 自己建立连接
60#FHsC_w|2. **但**当白名单内的其他设备（如内网本地服务器）也拥有公网 IPv4/IPv6 时，白名单外的 IP 可以**绕过 admin，直接连接本地服务器**
61#rmaiW6|3. 这导致仅靠 admin 端白名单无法保护关键的本地设备
62#47DEQp|
63#I6F5Lr|**解决方案**：二次开发"半中心化" admin + agent 架构：
64#3P5ytT|- **admin**（中心）：部署在云端，集中管控白名单
65#1Av3NU|- **agent**（半中心）：部署在每个关键本地设备上，从 admin 拉取白名单，本地阻断非法接入
66#KG4AoJ|- 即便有人通过公网 IP 直连 agent 设备，agent 也会拒绝非白名单连接
67#47DEQp|
68#DosgeQ|#### 角色与职责
69#47DEQp|
70#yM60mQ|| 角色 | 部署位置 | 职责 |
71#kJ7itv||------|----------|------|
72#yLa1lG|| admin | 云端公网服务器（如腾讯云） | 集中管理 IP 白名单、设备状态；提供 Web UI；对外公开接入点 |
73#PWTwO2|| agent | 内网关键服务器 × N | 从 admin 拉取白名单；本地执行白名单拦截；运行 easytier-core |
74#0T3Gz-|| 客户端 | 内部成员设备 / 外来人员设备 | 通过 easytier 客户端接入 VPN |
75#47DEQp|
76#izpmFB|#### 协作流程图
77#47DEQp|
78#8bkBhH|```
79#h3FWpv|              公网（Internet）
80#N880xj|                     │
81#kB5_Nm|       ┌─────────────┼─────────────┐
82#IqFZDD|       │             │             │
83#lLiNwT|   ┌───┴────┐   ┌────┴────┐   ┌────┴────┐
84#pMVsI-|   │ admin  │   │ 外部 IP │   │  ...    │
85#O4y0qN|   │ 云服务器│   │ 白名单外│   │  其他   │
86#Az-uxy|   └────────┘   └─────────┘   └─────────┘
87#e6rdx3|       │             ✗             ✗
88#65W2Lh|       │  （白名单外被 admin 阻断）
89#Nnwgp6|       │ ✓
90#plXQC4|       ├──────────────┐
91#Ol0JxZ|       │              │
92#ssr8E_|   ┌───┴────┐    ┌────┴────┐
93#JY8tgA|   │ agent  │◄───┤ 内部 IP │
94#a5LkoI|   │ 本地服务│    │ 白名单内│
95#Na31fI|   └────────┘    └─────────┘
96#FcI73b|       │  ✓
97#8E2yYr|       │  （agent 本地阻断白名单外）
98#owv5h8|       └──────────────┐
99#rKeIR-|                      │
100#WY__Q8|                  ┌───┴────┐
101#-s7Ncj|                  │ 外部 IP │
102#--q0ng|                  │ 白名单外│
103#5ZiZnS|                  └─────────┘
104#stFkTz|                     ✗
105#8P7DN8|            （agent 本地阻断）
106#47DEQp|
107#jVfgDr|虚线: 数据流
108#XgW0lG|✓: 允许  ✗: 阻断
109#8bkBhH|```
110#47DEQp|
111#SfjfWV|**关键时序**：
112#47DEQp|
113#xb11BA|1. admin 启动，监听 `0.0.0.0:11211`
114#b6yA_h|2. 客户端 / 外来设备尝试连接 admin → admin 检查白名单 → 通过 / 拒绝
115#y0605K|3. agent 启动 → 预置默认白名单（admin IP 兜底）→ 启动 easytier-core
116#7WpMeV|4. core 与 admin 建立 P2P 连接 → agent 定时从 admin 拉取白名单
117#0vWWHF|5. 客户端尝试直接连接 agent → agent 本地检查白名单 → 通过 / 拒绝
118#47DEQp|
119#LeIK_A|#### 关键设计：预置默认白名单
120#47DEQp|
121#oXXxdS|- agent 第一次启动时，`ip_whitelist.json` 不存在
122#BOi4Cr|- 入口脚本自动写入 `[{ip: "<admin虚拟IP>", hostname: null}]` 作为兜底
123#H2u0yD|- 这样 core 不会阻断 admin，P2P 连接能立即建立
124#RsikcZ|- sync-daemon 第一次拉取成功后，会用 admin 真实白名单覆盖默认值
125#47DEQp|
126#wbISpy|## 核心特性
127#47DEQp|
128#fQHYiJ|- **半中心化架构**：admin 集中管控 + agent 本地执行，双重白名单防护
129#VDbp_m|- **单镜像融合**：`easytier-core` + `easytier-web` + 自定义 Admin UI，一个容器搞定一切
130#D6Cd0A|- **官方客户端兼容**：实测 `easytier` 官方客户端 `2.3.2` 及 `2.4.5` 均可正常连接与管理
131#AiM6q2|- **IP 白名单机制**：仅允许指定 IP 的客户端建立连接，详见下文
132#YsZvtU|- **设备管理**：实时查看在线设备、系统/版本、心跳时间
133#agdldl|- **登录鉴权**：管理员账号 + JWT Token 认证
134#Zs97tr|- **多阶段构建**：Rust 编译 + pnpm 前端构建 + rust_embed 静态资源嵌入
135#47DEQp|
136#x76YqT|## 截图示例
137#47DEQp|
138#J14vqJ|### IP 白名单管理
139#47DEQp|
140#ZxljEg|![IP 白名单](eg/ip-whitelist.png)
141#47DEQp|
142#1faKpA|*IP 白名单管理界面：默认首页，可添加 / 删除白名单 IP，支持主机名绑定与解绑*
143#47DEQp|
144#XugOa0|### 设备管理
145#47DEQp|
146#kJ1Zd2|![设备管理](eg/devices.png)
147#47DEQp|
148#kzUMtK|*设备列表：展示已通过白名单接入的设备，包含机器 ID、主机名、虚拟 IP 和在线状态*
149#47DEQp|
150#UzLqj-|## Admin 节点部署
151#47DEQp|
152#s71GML|### 用途
153#47DEQp|
154#q3opSy|Admin 节点是半中心化架构的控制中心，部署在公网可达的服务器（如腾讯云）上。它负责：
155#SN9Et1|- 提供管理 Web UI（IP 白名单管理、设备管理）
156#6p04H_|- 作为 VPN 中介节点，连接各 agent 和客户端
157#dCqtAd|- 对外公开接入点，通过白名单过滤非法连接
158#47DEQp|
159#wtDI9_|### 快速开始
160#47DEQp|
161#F8bkga|最小启动命令：
162#47DEQp|
163#rTMjNc|```bash
164#0UsXWC|docker run -d --restart=always --privileged \
165#x8eXCM|  --name easytier-admin \
166#50fDoD|  --network host \
167#1Dqlpp|  -v $(pwd)/core.toml:/etc/easytier/core.toml \
168#3rMnZb|  -v $(pwd)/data:/data \
169#y-Yq29|  -e ET_ADMIN_PASSWORD=your-strong-password \
170#B2RZga|  easytier-admin:2.4.5
171#8bkBhH|```
172#47DEQp|
173#RU56x3|参数说明：
174#47DEQp|
175#1aK0CQ|| 参数 | 说明 |
176#Q1UY1c||------|------|
177#2FMX0_|| `--privileged` | `easytier-core` 需要创建 TUN 设备，必须使用特权模式 |
178#uDeyWG|| `--network host` | VPN 流量直接走主机网络栈 |
179#WvurlM|| `-v .../core.toml` | `easytier-core` 的配置文件（需提前准备好） |
180#dKpB3C|| `-v .../data` | 持久化数据库和运行时数据 |
181#I-50Fg|| `-e ET_ADMIN_PASSWORD` | Admin 管理员密码，**请务必修改** |
182#M2s10m|| `-e ET_ADMIN_SECRET` | JWT 签名密钥，建议设置（默认值为 `change-me-to-a-random-string`，不安全） |
183#47DEQp|
184#VH-Qlf|### core.toml 示例
185#47DEQp|
186#IFlRSq|`core.toml` 是 easytier-core 的主配置文件，挂载到容器的 `/etc/easytier/core.toml`。最简配置示例：
187#47DEQp|
188#E3uXOp|```toml
189#KZmrVz|ipv4 = "10.0.10.1/24"
190#BuulPS|listeners = [
191#_uyvOZ|    "tcp://0.0.0.0:22020",
192#g0U3Mz|    "udp://0.0.0.0:22020",
193#z64NQk|]
194#47DEQp|
195#3gQyNR|[network_identity]
196#ahTX2O|network_name = "your-network-name"       # ← 占位符，请改为自定义名称
197#wLC4yt|network_secret = "change-me-to-a-random-string"  # ← 占位符，请改为强随机密钥
198#47DEQp|
199#wCb8fa|[flags]
200#Thaouz|private_mode = true
201#8bkBhH|```
202#47DEQp|
203#UxzPdW|字段说明：
204#47DEQp|
205#HyP7-Z|- `ipv4` — 本节点在虚拟网络中的 IP 与子网长度（同子网内的客户端可以直接互通）
206#fq0kO5|- `listeners` — easytier-core 监听的协议与端口，客户端通过这些地址连接
207#JwdfvR|- `network_name` — 虚拟网络名称，同一网络的节点必须一致
208#jXX2qY|- `network_secret` — 虚拟网络密钥（用于加密通信），请改成强随机字符串
209#pOOwDK|- `private_mode` — 私有模式（默认不向外转发非本网络流量）
210#47DEQp|
211#seEGai|更多字段（如 `peers` 主动连接的节点、`rpc_portal` 管理 API 端口等）请参考 [easytier 官方文档](https://github.com/EasyTier/easytier)。
212#47DEQp|
213#Z6qkBH|启动后访问 Admin UI：`http://<your-server-ip>:11211/admin`
214#47DEQp|
215#lehMfz|### IP 白名单机制
216#47DEQp|
217#yxgaoe|在公网部署 EasyTier 节点时，任何知道连接地址的客户端都可以尝试接入。IP 白名单用于限制只有被授权 IP 的客户端才能与本地节点建立连接，防止未授权访问。
218#47DEQp|
219#L24zVV|#### 使用方式
220#47DEQp|
221#_DG4ix|登录 Admin UI 后，侧边栏默认进入「IP 白名单」页面。点击「＋ 添加 IP」按钮，在弹出框中输入目标客户端的 easytier 虚拟 IP 地址和备注（可选），即可将该 IP 加入白名单，建议手动分配客户端 IP。
222#47DEQp|
223#rZ527U|#### 可选字段
224#47DEQp|
225#OjzVdF|- **备注（comment）**：方便标识该 IP 对应的客户端，如"北京办公室出口"。
226#47DEQp|
227#JLGE3y|#### 主机名绑定
228#47DEQp|
229#GSoWhR|当某 IP 的客户端首次连接到节点时，Admin 界面会自动将该客户端的主机名（hostname）绑定到对应的白名单条目，方便运维人员辨认。如需清除绑定，点击该条目的「解绑」按钮即可。
230#47DEQp|
231#2fqg4Q|**行为说明**：
232#47DEQp|
233#3a8KNt|| 场景 | 行为 |
234#Q1UY1c||------|------|
235#cMyLRt|| 设备首次连接，白名单条目无主机名 | 自动绑定设备主机名到白名单条目 |
236#voG2yD|| 设备在线，admin 解除主机名绑定 | 核心会重新自动绑定（需先断开设备再解绑才能持久） |
237#bd66HW|| 设备断开，admin 解除主机名绑定 | 解绑成功后主机名保持为空，不会重新绑定 |
238#P0RlxG|| 设备重连（已解绑状态） | 重新自动绑定主机名 |
239#sYmIKz|| 空白名单（无任何条目） | 阻止所有连接 |
240#47DEQp|
241#LdDasj|#### 修复的 Bug
242#47DEQp|
243#wXiAEg|本次修复解决了以下问题：
244#47DEQp|
245#gLXwMw|1. **空白名单放行所有连接**：当白名单文件为空时，原本会跳过所有拦截检查。现已修复为空白名单=阻止所有连接。
246#47DEQp|
247#UcS6SX|2. **blocked_peers 内存状态不持久化**：被阻止的 peer 列表仅存储在内存中，容器重启后丢失。现已持久化到磁盘文件，重启后保留阻止状态。
248#47DEQp|
249#4sSDZ5|3. **白名单文件非原子写入**：`sqlite3 ... > file` 写入时会先截断文件，造成竞态条件。现已改为 `> file.tmp && mv file.tmp file` 原子写入。
250#47DEQp|
251#8CICw9|4. **自动绑定追踪去重阻止重新绑定**：入口脚本中的追踪文件 `/tmp/auto_bound_pairs.json` 永久记录已处理的 (ip, hostname) 对，导致 admin 解绑后无法重新绑定。现已移除追踪去重逻辑。
252#47DEQp|
253#7gXCGp|5. **自动绑定文件持久化导致离线设备重新绑定**：自动绑定文件 `/tmp/ip_hostname_auto.json` 未及时清理，导致已断开连接的设备被反复绑定。现已改为处理后立即删除文件，由核心根据设备在线状态决定是否重建。
254#47DEQp|
255#OVydSe|6. **先封禁后跳过连接清理导致存活连接残留**：当事件驱动任务先封禁 peer 后，定时任务因检测到 peer 已 blocked 而跳过关闭连接的操作，导致已建立的 UDP 隧道残留，非白名单设备仍能访问其他节点。现已修复为无论 peer 是否已标记 blocked，始终尝试关闭所有剩余连接。
256#47DEQp|
257#l0tY6B|#### 删除
258#47DEQp|
259#OQdXoY|通过白名单列表中的「删除」按钮移除条目。被移除的 IP 将无法再建立新的连接。
260#47DEQp|
261#IzOnL4|#### 存储
262#47DEQp|
263#LT1Bh2|白名单数据保存在 SQLite 数据库（`/data/easytier-admin.db`）中，随容器 `/data` 卷持久化。容器重启后白名单不会丢失。
264#47DEQp|
265#-T2Blm|#### 工作原理
266#47DEQp|
267#xTEE4G|EasyTier 的 IP 白名单实际生效在**网络连接建立时**，由 `easytier-core` 强制执行：
268#47DEQp|
269#ygqITJ|- `easytier-web`（即管理面板后端）只负责白名单的 CRUD 管理 API
270#qD6R03|- `entrypoint.sh` 每 30 秒将 SQLite 中的 `ip_whitelist` 表导出为 `/tmp/ip_whitelist.json`
271#Zw_0RS|- `easytier-core` 启动时通过 `IP_WHITELIST_FILE` 环境变量加载该 JSON 文件
272#tzT43x|- 当任意 peer 尝试建立连接时，`easytier-core` 检查对方虚拟 IP 是否在白名单内
273#5Yf_nG|- 命中白名单 → 正常握手；未命中 → 拒绝握手并记录日志
274#930rag|- 定时任务（每 30 秒）会检测已连接的非白名单设备并主动断开
275#47DEQp|
276#g7a1R4|#### 注意事项
277#47DEQp|
278#MW9ROy|- 白名单变更后，定时任务（每 30 秒）会自动检测并断开已建立连接的非白名单设备。
279#3YvSN-|- 白名单为空时，所有连接将被阻止（已修复此前的放行问题）。
280#47DEQp|
281#XugOa0|### 设备管理
282#47DEQp|
283#UQ1M1K|Admin UI 提供设备管理功能，实时展示已通过白名单接入的设备信息：
284#47DEQp|
285#Sc-GBd|- 机器 ID、主机名
286#3RDozC|- 虚拟 IP 地址
287#wtkjaR|- 系统版本、easytier 版本
288#qux512|- 心跳时间、在线状态
289#47DEQp|
290#ReTUvI|### 环境变量
291#47DEQp|
292#58-cI-|| 变量 | 默认值 | 说明 |
293#28EPHU||------|--------|------|
294#rWKlEu|| `IP_WHITELIST_FILE` | `/tmp/ip_whitelist.json` | IP 白名单文件路径 |
295#KY1NoW|| `BLOCKED_PEERS_FILE` | `/tmp/blocked_peers.json` | 被阻止 peer 持久化文件路径 |
296#-tRY5-|| `IP_HOSTNAME_AUTO_FILE` | `/tmp/ip_hostname_auto.json` | 主机名自动绑定文件路径 |
297#bdIlId|| `ET_ADMIN_PASSWORD` | — | Admin 管理员密码（**强烈建议设置**） |
298#zn2R2C|| `ET_ADMIN_SECRET` | `change-me-to-a-random-string` | JWT 签名密钥（建议修改） |
299#47DEQp|
300#47DEQp|
301#47DEQp|
302#ciax_W|## Agent 节点部署
303#47DEQp|
304#s71GML|### 用途
305#47DEQp|
306#olkAxI|对于"半中心化"部署：在关键内网服务器上部署 agent 容器，由 admin 统一管控 IP 白名单。即使该服务器有公网 IP，也只允许 admin 授权的设备访问。
307#47DEQp|
308#3KL90K|### 与 admin 的关系
309#47DEQp|
310#XAAN7P|- admin 部署在云端（或公网可达的服务器），运行 `easytier-web`
311#7G0VXx|- agent 部署在内网关键服务器上，运行 `easytier-core` + `easytier-whitelist-sync`
312#oYFiQU|- agent 从 admin 拉取白名单，本地阻断白名单外的设备
313#47DEQp|
314#xCLYxo|### 虚拟 IP 命名约定（.1 后缀）
315#47DEQp|
316#uCz4bV|**admin 必须使用 `.1` 后缀**，agent 使用其他后缀：
317#47DEQp|
318#noiE5l|| 角色 | 虚拟 IP | 说明 |
319#x8pw5q||------|---------|------|
320#0q2BCP|| admin | `10.0.10.1/24` | 固定为子网的 `.1` |
321#3cGf87|| agent #1 | `10.0.10.2/24` | 内网服务器 1 |
322#tK-m3v|| agent #2 | `10.0.10.3/24` | 内网服务器 2 |
323#v0bALt|| 普通客户端 | `10.0.10.x` | 其他设备 |
324#47DEQp|
325#m4qPoj|agent 启动时会从自己的 `core.toml` 读取 `ipv4` 字段（如 `10.0.10.2/24`），自动取前三段（`10.0.10`）拼上 `.1` → 得到 admin 的默认 IP（`10.0.10.1`），写入默认白名单。
326#47DEQp|
327#xvnD-c|### agent 端 core.toml 示例
328#47DEQp|
329#E3uXOp|```toml
330#Gxm1E5|ipv4 = "10.0.10.2/24"
331#BuulPS|listeners = [
332#_uyvOZ|    "tcp://0.0.0.0:22020",
333#g0U3Mz|    "udp://0.0.0.0:22020",
334#z64NQk|]
335#47DEQp|
336#3gQyNR|[network_identity]
337#g-rX_3|network_name = "your-network-name"       # ← 占位符，必须与 admin 端一致
338#TeQYSt|network_secret = "change-me-to-a-random-string"  # ← 占位符，必须与 admin 端一致
339#47DEQp|
340#kaOTDv|[[peer]]
341#eRr1eX|uri = "tcp://<admin公网IP>:22020"
342#47DEQp|
343#wCb8fa|[flags]
344#Thaouz|private_mode = true
345#8bkBhH|```
346#47DEQp|
347#UxzPdW|字段说明：
348#4BQaQH|- `ipv4`：agent 自己的虚拟 IP，**必须与 admin 同子网**，且不能是 `.1`
349#KTn0CD|- `[[peer]]`：admin 的公网/内网 IP + easytier 端口（默认 22020）
350#M33gDp|- `network_name` / `network_secret`：必须与 admin 端完全一致
351#47DEQp|
352#KKq652|### 白名单同步机制
353#47DEQp|
354#8bkBhH|```
355#Hhr98J|1. 读取 core.toml 的 ipv4 → 自动算出 admin 默认 IP（.1 后缀）
356#Cc1lXn|2. 写入 /data/ip_whitelist.json 作为预置白名单
357#DA5637|3. 启动 whitelist-sync-daemon（向 admin 拉取真实白名单）
358#RF-9VI|4. 启动 easytier-core
359#RdEolD|5. core 每 30s 检测白名单 + 阻断非白名单设备
360#FGAMEA|6. sync-daemon 每 30s 拉取 admin 白名单覆盖本地文件
361#8bkBhH|```
362#47DEQp|
363#JVs39y|**admin 失联时的行为**：
364#SiFNsb|- sync-daemon 检测到 admin 不可达 → **不修改本地白名单文件**
365#a8l-A9|- core 继续使用最后一次同步成功的白名单
366#RjDKmP|- 安全性不降低（不会因为 admin 失联而放行新设备）
367#47DEQp|
368#Mr6GVj|### 启动流程
369#47DEQp|
370#7eGG6C|agent 完整启动流程：
371#47DEQp|
372#vD8OC1|1. 入口脚本读取 `core.toml` 的 `ipv4`，自动推算 admin IP（`.1` 后缀）
373#OsCiCQ|2. 若 `/data/ip_whitelist.json` 不存在，写入默认白名单（仅含 admin IP）
374#4LIRIh|3. 启动 `whitelist-sync-daemon` 后台进程
375#Sk2mUC|4. 启动 `easytier-core`
376#fxk7U_|5. core 建立与 admin 的 P2P 连接
377#uiLp9f|6. sync-daemon 定时从 admin 拉取白名单，覆盖本地文件
378#47DEQp|
379#ReTUvI|### 环境变量
380#47DEQp|
381#58-cI-|| 变量 | 默认值 | 说明 |
382#28EPHU||------|--------|------|
383#BOFPVU|| `WHITELIST_SYNC_URL` | （必填）| admin 公开白名单端点，如 `http://10.0.10.1:11211/api/v1/public/whitelist.json` |
384#LXU9Ie|| `IP_WHITELIST_FILE` | `/data/ip_whitelist.json` | 本地白名单文件路径 |
385#KeKoxG|| `WHITELIST_SYNC_INTERVAL` | `30` | 拉取间隔（秒）|
386#68EtGO|| `WHITELIST_DEFAULT_IPS` | 自动检测 | 预置白名单的 IP 列表（逗号分隔），默认从 core.toml 自动算 admin 的 `.1` 后缀 |
387#zIkF7S|| `CORE_CONFIG` | `/etc/easytier/core.toml` | core 配置文件路径 |
388#47DEQp|
389#60-Pds|### 构建与启动
390#47DEQp|
391#rTMjNc|```bash
392#bgWP1J|# 1. 构建 agent 镜像
393#jw8pC-|docker build -t easytier-agent:latest -f agent.Dockerfile .
394#47DEQp|
395#HWMUH-|# 2. 准备 core.toml（见上节示例）
396#47DEQp|
397#-ip2ij|# 3. 启动 agent 容器
398#ojlfEE|docker run -d \
399#UKuRsr|  --name easytier-agent \
400#B-kZQG|  --restart unless-stopped \
401#50fDoD|  --network host \
402#YKwbih|  --cap-add NET_ADMIN \
403#MDolmc|  --device /dev/net/tun \
404#OIGbOt|  -v /opt/easytier-agent/config/core.toml:/etc/easytier/core.toml:ro \
405#cH-9Ud|  -v easytier-agent-data:/data \
406#9OMu4W|  -e WHITELIST_SYNC_URL=http://10.0.10.1:11211/api/v1/public/whitelist.json \
407#MYmvFG|  -e WHITELIST_SYNC_INTERVAL=30 \
408#mUAg9u|  easytier-agent:latest
409#47DEQp|
410#Jk1Ni_|# 4. 验证
411#O27Cf3|docker logs -f easytier-agent
412#VXePP-|# 应看到：
413#85lw65|#   [agent] auto-detected admin IP from core.toml: 10.0.10.1 (agent=10.0.10.2)
414#f8VGEu|#   [agent] initializing default whitelist (kills chicken-and-egg)
415#1x8oTj|#   [agent] default whitelist: [{"ip":"10.0.10.1","hostname":null}]
416#NfJYFq|#   whitelist-sync-daemon starting: ...
417#8OwnbJ|#   Whitelist synced: N entries
418#8bkBhH|```
419#47DEQp|
420#Xlq3tX|### 重新生成默认白名单
421#47DEQp|
422#ijPU3b|如果需要重新触发默认白名单逻辑（例如网络重命名），删除数据卷：
423#47DEQp|
424#rTMjNc|```bash
425#A9wpVF|docker stop easytier-agent
426#OnOOsl|docker rm easytier-agent
427#G_Sr2D|docker volume rm easytier-agent-data
428#6NMkwI|# 然后重新 docker run
429#8bkBhH|```
430#47DEQp|
431#X1AAmN|## 兼容性与版本
432#47DEQp|
433#A72gb0|本镜像内置的 `easytier-core` 与 `easytier-web` 与官方版本保持一致，仅以下文件有定制修改：
434#47DEQp|
435#fROwi6|- `easytier-web/admin-frontend/index.html` — 自定义管理 UI（IP 白名单、设备管理、时间本地化）
436#6E3Mf9|- `Dockerfile` — 增加了 `tzdata` 和 `ENV TZ=Asia/Shanghai` 配置
437#47DEQp|
438#O2n2fe|实测兼容的官方客户端版本：
439#47DEQp|
440#icoT-e|| 客户端版本 | 状态 |
441#kFf8YZ||-----------|------|
442#svadrI|| 2.3.2     | 正常 |
443#nj2Sth|| 2.4.5     | 正常 |
444#47DEQp|
445#lh1-4f|## 构建镜像
446#47DEQp|
447#gBkQXM|本镜像采用 Docker 多阶段构建：
448#47DEQp|
449#6QaWUE|1. **Builder 阶段**：Rust 1.89 编译 `easytier-core` 和 `easytier-web`；pnpm 编译前端（`frontend-lib` + `frontend`）；`rust_embed` 将 `admin-frontend/index.html` 嵌入二进制
450#_rJtYZ|2. **Runtime 阶段**：`debian:bookworm-slim` 基础镜像，安装运行时依赖，复制编译产物
451#47DEQp|
452#gX_EZt|构建命令：
453#47DEQp|
454#rTMjNc|```bash
455#xReK-R|docker build --platform linux/amd64 -t easytier-admin:2.4.5 .
456#8bkBhH|```
457#47DEQp|
458#B5ckTg|首次构建需要下载 Rust crate 依赖并完整编译，耗时约 10–30 分钟（视网络和机器性能而定）。后续构建会利用 Docker 层缓存，仅重新编译变更部分。
459#47DEQp|
460#ZrcCRO|## 一键脚本
461#47DEQp|
462#1bwCuP|仓库根目录提供 `build-and-run.sh`（Linux amd64），支持一键构建并启动容器：
463#47DEQp|
464#rTMjNc|```bash
465#ZZ_zW7|./build-and-run.sh                                                      # 默认参数
466#mpL6Sf|./build-and-run.sh --tag 2.4.5 --password your-password --container my-easytier
467#8bkBhH|```
468#47DEQp|
469#z_LSEy|所有可调参数：
470#47DEQp|
471#ckRseV|| 参数 | 说明 | 默认值 |
472#Zteqcb||------|------|--------|
473#L8L8e1|| `--image` | 镜像名称 | `easytier-admin` |
474#h9vuwY|| `--tag` | 镜像标签 | `2.4.5` |
475#Rnsph5|| `--container` | 容器名称 | `easytier-admin` |
476#p13jLy|| `--password` | Admin 密码 | `changeme-please` |
477#EKaSoH|| `--web-port` | Admin UI 端口（仅显示用） | `11211` |
478#s87WdJ|| `--vpn-port` | VPN 监听端口（仅显示用） | `22020` |
479#G52l0j|| `--no-cache` | 不使用 Docker 构建缓存 | 关闭 |
480#47DEQp|
481#xSldlR|## 目录结构
482#47DEQp|
483#8bkBhH|```
484#zbTuKu|.
485#XK0mpi|├── Dockerfile              # 多阶段构建文件
486#Q_lSF2|├── agent.Dockerfile       # agent 节点镜像（半中心化部署）
487#YJmtR4|├── entrypoint.sh           # 容器入口脚本
488#FaB6Zc|├── build-and-run.sh        # 一键构建运行脚本
489#hUenHg|├── README.md               # 本文档
490#Dm3O1B|├── .gitignore
491#CjXFCu|├── LICENSE                 # 上游 LGPL-3.0 许可证
492#hhpxgH|├── Cargo.toml              # Rust workspace 根配置
493#SalN2g|├── Cargo.lock
494#O3bPW2|├── pnpm-workspace.yaml     # pnpm monorepo 配置
495#vLIrLO|├── pnpm-lock.yaml
496#RZBhAW|├── easytier/               # easytier-core 源码
497#ei_v68|│   ├── Cargo.toml
498#oZHzVr|│   └── src/
499#Dny7HH|├── easytier-rpc-build/     # protobuf RPC 编译辅助
500#ei_v68|│   ├── Cargo.toml
501#oZHzVr|│   └── src/
502#my9-C2|├── easytier-web/           # 管理后台源码
503#ei_v68|│   ├── Cargo.toml
504#i4-TQt|│   ├── src/                # Rust 后端（REST API、IP 白名单、SQLite）
505#tPup5f|│   ├── admin-frontend/     # 自定义管理 UI（HTML+JS，被 rust_embed 嵌入二进制）
506#IbqGs9|│   ├── frontend/           # 官方 Web 前端（Vue+TS，编译后作为依赖）
507#gadNq4|│   └── frontend-lib/       # 前端组件库
508#BZH1im|├── vendor/                 # 离线构建的 vendored Rust 依赖
509#gW82sD|└── .cargo/                 # Cargo 构建配置
510#8bkBhH|```
511#47DEQp|
512#ZVfp8_|## 常见问题（FAQ）
513#47DEQp|
514#-cRN8e|**Q: Admin 默认账号是什么？**
515#NQwtol|A: 用户名 `admin`，密码由 `ET_ADMIN_PASSWORD` 环境变量设置。首次启动后通过 Admin UI 注册。
516#47DEQp|
517#O1W0PP|**Q: 忘记密码怎么办？**
518#Gcp7CX|A: 删除 `/data/easytier-admin.db` 文件后重启容器，系统会重新初始化（**注意：会丢失所有 IP 白名单和设备数据**）。
519#47DEQp|
520#wt4VrT|**Q: 容器内时区是什么？**
521#xBQKOo|A: 已设为 `Asia/Shanghai`，Admin UI 中所有时间显示为北京时间（UTC+8）。
522#47DEQp|
523#UrLTV2|**Q: 如何升级 easytier-core 版本？**
524#GLu21b|A: 更新 `easytier/Cargo.toml` 中的版本号，然后重新构建镜像。
525#47DEQp|
526#as2RBp|**Q: 为什么需要 `--privileged`？**
527#BdrSkt|A: `easytier-core` 需要创建 TUN 网络设备，这需要特权模式。
528#47DEQp|
529#pFt8Tp|**Q: agent 无法同步白名单怎么办？**
530#9quEII|A: 检查 `WHITELIST_SYNC_URL` 是否正确、admin 是否可达。admin 失联时 agent 会使用最后一次同步的白名单，不会放行新设备。
531#47DEQp|
532#d4XDlx|## 许可与致谢
533#47DEQp|
534#wq6wT7|本项目基于 [EasyTier](https://github.com/EasyTier/EasyTier) 进行二次开发，遵循上游 [LGPL-3.0](LICENSE) 许可证。
535#47DEQp|
536#gy7y6A|感谢 EasyTier 开源社区提供的优秀组网方案。
537#47DEQp|

## [assistant] +058391-06-28T00:07:06.000Z
[thinking] Now I have the full README.md content. Let me plan the updates:

1. **IP 白名单机制章节** (around line 265-275, "工作原理" section) — Add a note about admin auto-registering its own IP

2. **Agent 环境变量** (around line 379-388) — Add RUST_LOG default value

3. **修复的 Bug 章节** (around line 241-256) — Add the two new bugs

Let me plan the exact edits:

### Edit 1: Add admin auto-register to "工作原理" (after line 274, before the "注意事项" section)

Looking at lines 265-279:
- Line 265: `#### 工作原理`
- Line 267-274: bullet points explaining the flow
- Line 276: `#### 注意事项`

I should add a line about admin auto-registering itself. A good place would be after line 274.

### Edit 2: Add two more Bug entries to "修复的 Bug"

Lines 241-257:
- Line 241: `#### 修复的 Bug`
- Line 243: intro line
- Lines 245-255: Bug 1-6
- Line 257: `#### 删除`

I need to add Bug 7 and Bug 8 after Bug 6 (line 255).

### Edit 3: Add RUST_LOG to agent environment variables

Lines 379-388:
- Line 379: `### 环境变量`
- Lines 381-388: table

I need to add a row for RUST_LOG. Let me check the exact lines.

Wait, looking at the table structure:
```
| 变量 | 默认值 | 说明 |
|------|--------|------|
| `WHITELIST_SYNC_URL` | （必填）| ... |
| `IP_WHITELIST_FILE` | `/data/ip_whitelist.json` | ... |
| `WHITELIST_SYNC_INTERVAL` | `30` | ... |
| `WHITELIST_DEFAULT_IPS` | 自动检测 | ... |
| `CORE_CONFIG` | `/etc/easytier/core.toml` | ... |
```

I need to add `RUST_LOG` row.

Now, let me also think about whether to add a FAQ entry. The analyst suggested "新增 FAQ：`[BIND_DEBUG]` 日志含义及排查". I'll add that too.

Let me now craft the edits.

#### Edit 1: "工作原理" section - add auto-register line

After line 274 (`- 定时任务（每 30 秒）会检测已连接的非白名单设备并主动断开`), I need to add a new bullet point about admin auto-register.

#### Edit 2: "修复的 Bug" - add Bug 7 and Bug 8

After line 255, before line 257 (`#### 删除`).

But I need to be careful - line 255 ends Bug 6. Let me craft the text for Bug 7 and Bug 8.

Bug 7: `INSERT OR IGNORE` 缺少 NOT NULL 字段 `created_by`/`created_at` 导致 admin 自身 IP
--- todowrite({"todos":[{"content":"Add admin auto-register bullet to 工作原理 section","status":"in_progress","priority":"high"},{"content":"Add Bug 7 (INSERT OR IGNORE) and Bug 8 (close_peer) to 修复的 Bug section","status":"pending","priority":"high"},{"content":"Add RUST_LOG row to Agent 环境变量 table","status":"pending","priority":"high"},{"content":"Add FAQ entry for [BIND_DEBUG] auto-bound hostname","status":"pending","priority":"medium"}]}) [completed]
[
  {
    "content": "Add admin auto-register bullet to 工作原理 section",
    "status": "in_progress",
    "priority": "high"
  },
  {
    "content": "Add Bug 7 (INSERT OR IGNORE) and Bug 8 (close_peer) to 修复的 Bug section",
    "status": "pending",
    "priority": "high"
  },
  {
    "content": "Add RUST_LOG row to Agent 环境变量 table",
    "status": "pending",
    "priority": "high"
  },
  {
    "content": "Add FAQ entry for [BIND_DEBUG] auto-bound hostname",
    "status": "pending",
    "priority": "medium"
  }
]

## [assistant] +058391-06-28T07:02:50.000Z
[thinking] Now let me apply all 4 edits. I'll start with the first one.
--- edit({"filePath":"/mnt/nvme2t_2/easytier/README.md","oldString":"|- 定时任务（每 30 秒）会检测已连接的非白名单设备并主动断开\n\n#### 注意事项","newString":"|- 定时任务（每 30 秒）会检测已连接的非白名单设备并主动断开\n|- admin 容器启动时 entrypoint.sh 自动从 core.toml 读取自身 IP，INSERT OR IGNORE 写入白名单（附带 hostname、created_by、created_at），确保 admin 自身 IP 始终在白名单中，避免 agent 反复触发 auto-bind\n\n#### 注意事项"}) [error]
ERROR: Could not find oldString in the file. It must match exactly, including whitespace, indentation, and line endings.

## [assistant] +058391-06-28T07:49:11.000Z
[thinking] The edit failed due to string mismatch. It could be whitespace or tab vs space issues. Let me re-read the exact content around those lines to see the exact characters.
--- read({"filePath":"/mnt/nvme2t_2/easytier/README.md","offset":265,"limit":20}) [completed]
<path>/mnt/nvme2t_2/easytier/README.md</path>
<type>file</type>
<content>
265: #### 工作原理
266: 
267: EasyTier 的 IP 白名单实际生效在**网络连接建立时**，由 `easytier-core` 强制执行：
268: 
269: - `easytier-web`（即管理面板后端）只负责白名单的 CRUD 管理 API
270: - `entrypoint.sh` 每 30 秒将 SQLite 中的 `ip_whitelist` 表导出为 `/tmp/ip_whitelist.json`
271: - `easytier-core` 启动时通过 `IP_WHITELIST_FILE` 环境变量加载该 JSON 文件
272: - 当任意 peer 尝试建立连接时，`easytier-core` 检查对方虚拟 IP 是否在白名单内
273: - 命中白名单 → 正常握手；未命中 → 拒绝握手并记录日志
274: - 定时任务（每 30 秒）会检测已连接的非白名单设备并主动断开
275: 
276: #### 注意事项
277: 
278: - 白名单变更后，定时任务（每 30 秒）会自动检测并断开已建立连接的非白名单设备。
279: - 白名单为空时，所有连接将被阻止（已修复此前的放行问题）。
280: 
281: ### 设备管理
282: 
283: Admin UI 提供设备管理功能，实时展示已通过白名单接入的设备信息：
284: 

(Showing lines 265-284 of 536. Use offset=285 to continue.)
</content>

## [assistant] +058391-06-28T08:29:18.000Z
[thinking] I see now. The lines in the read_with_hash output showed `#930rag|` prefix, but that's the hash annotation. The actual content is just the line text. Let me try the edit again with the exact content.

The old string needs to match exactly. From the read output, line 274 is:
```
- 定时任务（每 30 秒）会检测已连接的非白名单设备并主动断开
```
Then empty line at 275, then at 276:
```
#### 注意事项
```

The issue might be trailing whitespace or something. Let me try checking with cat to see the raw bytes. Actually, let me just try exact match.
--- invalid({"tool":"bash","error":"Model tried to call unavailable tool 'bash'. Available tools: brainstorm, declare_principle, delegate_cancel, delegate_status, edit, flowcraft_status, gigpow, glob, grep, hashline_edit, invalid, job_list, job_status, list_principles, read, read_with_hash, run_skill, skill, skill_index, todowrite, webfetch, write."}) [completed]
The arguments provided to the tool are invalid: Model tried to call unavailable tool 'bash'. Available tools: brainstorm, declare_principle, delegate_cancel, delegate_status, edit, flowcraft_status, gigpow, glob, grep, hashline_edit, invalid, job_list, job_status, list_principles, read, read_with_hash, run_skill, skill, skill_index, todowrite, webfetch, write.

## [assistant] +058391-06-28T09:18:04.000Z
[thinking] OK no bash tool. Let me try the edit again, maybe the issue is whitespace. Let me check by reading the raw bytes carefully.
Let me check the exact bytes:
--- grep({"pattern":"定时任务（每 30 秒）会检测已连接的非白名单设备并主动断开","path":"/mnt/nvme2t_2/easytier","include":"README.md"}) [completed]
Found 1 matches
/mnt/nvme2t_2/easytier/README.md:
  Line 274: - 定时任务（每 30 秒）会检测已连接的非白名单设备并主动断开


## [assistant] +058391-06-28T10:06:50.000Z
[thinking] The file content looks fine. Let me try the edit again, being very precise about the string. Maybe I had some invisible characters or spacing issues. Let me re-read the area one more time to make sure and try again.
--- edit({"filePath":"/mnt/nvme2t_2/easytier/README.md","oldString":"- 定时任务（每 30 秒）会检测已连接的非白名单设备并主动断开\n\n#### 注意事项","newString":"- 定时任务（每 30 秒）会检测已连接的非白名单设备并主动断开\n- admin 容器启动时 entrypoint.sh 自动从 core.toml 读取自身 IP，INSERT OR IGNORE 写入白名单（附带 hostname、created_by、created_at），确保 admin 自身 IP 始终在白名单中，避免 agent 反复触发 auto-bind\n\n#### 注意事项"}) [completed]
Edit applied successfully.

## [assistant] +058391-06-28T11:07:13.000Z
[thinking] Edit 1 succeeded. Now let me apply edit 2 (修复的 Bug section).
--- todowrite({"todos":[{"priority":"high","content":"Add admin auto-register bullet to 工作原理 section","status":"completed"},{"priority":"high","content":"Add Bug 7 (INSERT OR IGNORE) and Bug 8 (close_peer) to 修复的 Bug section","status":"in_progress"},{"priority":"high","content":"Add RUST_LOG row to Agent 环境变量 table","status":"pending"},{"priority":"medium","content":"Add FAQ entry for [BIND_DEBUG] auto-bound hostname","status":"pending"}]}) [completed]
[
  {
    "content": "Add admin auto-register bullet to 工作原理 section",
    "status": "completed",
    "priority": "high"
  },
  {
    "content": "Add Bug 7 (INSERT OR IGNORE) and Bug 8 (close_peer) to 修复的 Bug section",
    "status": "in_progress",
    "priority": "high"
  },
  {
    "content": "Add RUST_LOG row to Agent 环境变量 table",
    "status": "pending",
    "priority": "high"
  },
  {
    "content": "Add FAQ entry for [BIND_DEBUG] auto-bound hostname",
    "status": "pending",
    "priority": "medium"
  }
]

## [assistant] +058391-06-28T11:53:44.000Z
[thinking] Now edit 2 - let me read the current file around the 修复的 Bug section to get the exact content after the first edit.
--- read({"filePath":"/mnt/nvme2t_2/easytier/README.md","offset":241,"limit":25}) [completed]
<path>/mnt/nvme2t_2/easytier/README.md</path>
<type>file</type>
<content>
241: #### 修复的 Bug
242: 
243: 本次修复解决了以下问题：
244: 
245: 1. **空白名单放行所有连接**：当白名单文件为空时，原本会跳过所有拦截检查。现已修复为空白名单=阻止所有连接。
246: 
247: 2. **blocked_peers 内存状态不持久化**：被阻止的 peer 列表仅存储在内存中，容器重启后丢失。现已持久化到磁盘文件，重启后保留阻止状态。
248: 
249: 3. **白名单文件非原子写入**：`sqlite3 ... > file` 写入时会先截断文件，造成竞态条件。现已改为 `> file.tmp && mv file.tmp file` 原子写入。
250: 
251: 4. **自动绑定追踪去重阻止重新绑定**：入口脚本中的追踪文件 `/tmp/auto_bound_pairs.json` 永久记录已处理的 (ip, hostname) 对，导致 admin 解绑后无法重新绑定。现已移除追踪去重逻辑。
252: 
253: 5. **自动绑定文件持久化导致离线设备重新绑定**：自动绑定文件 `/tmp/ip_hostname_auto.json` 未及时清理，导致已断开连接的设备被反复绑定。现已改为处理后立即删除文件，由核心根据设备在线状态决定是否重建。
254: 
255: 6. **先封禁后跳过连接清理导致存活连接残留**：当事件驱动任务先封禁 peer 后，定时任务因检测到 peer 已 blocked 而跳过关闭连接的操作，导致已建立的 UDP 隧道残留，非白名单设备仍能访问其他节点。现已修复为无论 peer 是否已标记 blocked，始终尝试关闭所有剩余连接。
256: 
257: #### 删除
258: 
259: 通过白名单列表中的「删除」按钮移除条目。被移除的 IP 将无法再建立新的连接。
260: 
261: #### 存储
262: 
263: 白名单数据保存在 SQLite 数据库（`/data/easytier-admin.db`）中，随容器 `/data` 卷持久化。容器重启后白名单不会丢失。
264: 
265: #### 工作原理

(Showing lines 241-265 of 537. Use offset=266 to continue.)
</content>

## [assistant] +058391-06-28T12:32:09.000Z
[thinking] Good. Now I need to insert Bug 7 and Bug 8 after line 255 (Bug 6). The exact old string to match is:

```
6. **先封禁后跳过连接清理导致存活连接残留**：当事件驱动任务先封禁 peer 后，定时任务因检测到 peer 已 blocked 而跳过关闭连接的操作，导致已建立的 UDP 隧道残留，非白名单设备仍能访问其他节点。现已修复为无论 peer 是否已标记 blocked，始终尝试关闭所有剩余连接。

#### 删除
```

I'll replace this with the same bug 6 text, followed by bugs 7 and 8, then the `#### 删除` header.
--- edit({"filePath":"/mnt/nvme2t_2/easytier/README.md","oldString":"6. **先封禁后跳过连接清理导致存活连接残留**：当事件驱动任务先封禁 peer 后，定时任务因检测到 peer 已 blocked 而跳过关闭连接的操作，导致已建立的 UDP 隧道残留，非白名单设备仍能访问其他节点。现已修复为无论 peer 是否已标记 blocked，始终尝试关闭所有剩余连接。\n\n#### 删除","newString":"6. **先封禁后跳过连接清理导致存活连接残留**：当事件驱动任务先封禁 peer 后，定时任务因检测到 peer 已 blocked 而跳过关闭连接的操作，导致已建立的 UDP 隧道残留，非白名单设备仍能访问其他节点。现已修复为无论 peer 是否已标记 blocked，始终尝试关闭所有剩余连接。\n7. **admin 自身 IP 静默插入失败**：`entrypoint.sh` 的 `INSERT OR IGNORE` 缺少 NOT NULL 字段 `created_by`/`created_at`，导致 admin 自身 IP 无法写入白名单。agent 每 30 秒发现 admin IP 无 hostname，反复触发 `[BIND_DEBUG] auto-bound hostname` 日志。现已补全字段。\n8. **close_peer_conn 使用默认连接 ID 导致关闭静默失败**：peer 有多连接时 `default_conn_id` 被后台任务清零为全零，`close_peer_conn(零ID)` 静默跳过。现已改用 `close_peer` 直接移除整个 peer。\n\n#### 删除"}) [completed]
Edit applied successfully.

## [assistant] +058391-06-28T13:46:05.000Z
[thinking] Good. Now Edit 3 - add RUST_LOG to the Agent environment variables table. Let me read the current section to get exact content.
--- todowrite({"todos":[{"priority":"high","content":"Add admin auto-register bullet to 工作原理 section","status":"completed"},{"priority":"high","content":"Add Bug 7 (INSERT OR IGNORE) and Bug 8 (close_peer) to 修复的 Bug section","status":"completed"},{"priority":"high","content":"Add RUST_LOG row to Agent 环境变量 table","status":"in_progress"},{"priority":"medium","content":"Add FAQ entry for [BIND_DEBUG] auto-bound hostname","status":"pending"}]}) [completed]
[
  {
    "content": "Add admin auto-register bullet to 工作原理 section",
    "status": "completed",
    "priority": "high"
  },
  {
    "content": "Add Bug 7 (INSERT OR IGNORE) and Bug 8 (close_peer) to 修复的 Bug section",
    "status": "completed",
    "priority": "high"
  },
  {
    "content": "Add RUST_LOG row to Agent 环境变量 table",
    "status": "in_progress",
    "priority": "high"
  },
  {
    "content": "Add FAQ entry for [BIND_DEBUG] auto-bound hostname",
    "status": "pending",
    "priority": "medium"
  }
]

## [assistant] +058391-06-28T14:25:59.000Z
[thinking] Let me read the agent environment variables section to get exact content.
--- read({"filePath":"/mnt/nvme2t_2/easytier/README.md","offset":379,"limit":20}) [completed]
<path>/mnt/nvme2t_2/easytier/README.md</path>
<type>file</type>
<content>
379: 5. core 建立与 admin 的 P2P 连接
380: 6. sync-daemon 定时从 admin 拉取白名单，覆盖本地文件
381: 
382: ### 环境变量
383: 
384: | 变量 | 默认值 | 说明 |
385: |------|--------|------|
386: | `WHITELIST_SYNC_URL` | （必填）| admin 公开白名单端点，如 `http://10.0.10.1:11211/api/v1/public/whitelist.json` |
387: | `IP_WHITELIST_FILE` | `/data/ip_whitelist.json` | 本地白名单文件路径 |
388: | `WHITELIST_SYNC_INTERVAL` | `30` | 拉取间隔（秒）|
389: | `WHITELIST_DEFAULT_IPS` | 自动检测 | 预置白名单的 IP 列表（逗号分隔），默认从 core.toml 自动算 admin 的 `.1` 后缀 |
390: | `CORE_CONFIG` | `/etc/easytier/core.toml` | core 配置文件路径 |
391: 
392: ### 构建与启动
393: 
394: ```bash
395: # 1. 构建 agent 镜像
396: docker build -t easytier-agent:latest -f agent.Dockerfile .
397: 
398: # 2. 准备 core.toml（见上节示例）

(Showing lines 379-398 of 539. Use offset=399 to continue.)
</content>

## [assistant] +058391-06-28T15:04:01.000Z
[thinking] I need to add `RUST_LOG` row after the `CORE_CONFIG` row. The old string is:

```
| `CORE_CONFIG` | `/etc/easytier/core.toml` | core 配置文件路径 |
```

And the new string adds the RUST_LOG row after it.
--- edit({"filePath":"/mnt/nvme2t_2/easytier/README.md","oldString":"| `CORE_CONFIG` | `/etc/easytier/core.toml` | core 配置文件路径 |","newString":"| `CORE_CONFIG` | `/etc/easytier/core.toml` | core 配置文件路径 |\n| `RUST_LOG` | `easytier=error` | tracing 日志级别，只输出 ERROR；Format 2 日志（`[uuid] message`）不受此影响 |"}) [completed]
Edit applied successfully.

## [assistant] +058391-06-28T15:55:17.000Z
[thinking] Now Edit 4 - FAQ entry. Let me read the FAQ section to get the exact content.
--- todowrite({"todos":[{"priority":"high","content":"Add admin auto-register bullet to 工作原理 section","status":"completed"},{"priority":"high","content":"Add Bug 7 (INSERT OR IGNORE) and Bug 8 (close_peer) to 修复的 Bug section","status":"completed"},{"priority":"high","content":"Add RUST_LOG row to Agent 环境变量 table","status":"completed"},{"priority":"medium","content":"Add FAQ entry for [BIND_DEBUG] auto-bound hostname","status":"in_progress"}]}) [completed]
[
  {
    "content": "Add admin auto-register bullet to 工作原理 section",
    "status": "completed",
    "priority": "high"
  },
  {
    "content": "Add Bug 7 (INSERT OR IGNORE) and Bug 8 (close_peer) to 修复的 Bug section",
    "status": "completed",
    "priority": "high"
  },
  {
    "content": "Add RUST_LOG row to Agent 环境变量 table",
    "status": "completed",
    "priority": "high"
  },
  {
    "content": "Add FAQ entry for [BIND_DEBUG] auto-bound hostname",
    "status": "in_progress",
    "priority": "medium"
  }
]

## [assistant] +058391-06-28T16:33:17.000Z
--- read({"filePath":"/mnt/nvme2t_2/easytier/README.md","offset":525,"limit":20}) [completed]
<path>/mnt/nvme2t_2/easytier/README.md</path>
<type>file</type>
<content>
525: A: 已设为 `Asia/Shanghai`，Admin UI 中所有时间显示为北京时间（UTC+8）。
526: 
527: **Q: 如何升级 easytier-core 版本？**
528: A: 更新 `easytier/Cargo.toml` 中的版本号，然后重新构建镜像。
529: 
530: **Q: 为什么需要 `--privileged`？**
531: A: `easytier-core` 需要创建 TUN 网络设备，这需要特权模式。
532: 
533: **Q: agent 无法同步白名单怎么办？**
534: A: 检查 `WHITELIST_SYNC_URL` 是否正确、admin 是否可达。admin 失联时 agent 会使用最后一次同步的白名单，不会放行新设备。
535: 
536: ## 许可与致谢
537: 
538: 本项目基于 [EasyTier](https://github.com/EasyTier/EasyTier) 进行二次开发，遵循上游 [LGPL-3.0](LICENSE) 许可证。
539: 
540: 感谢 EasyTier 开源社区提供的优秀组网方案。

(End of file - total 540 lines)
</content>

## [assistant] +058391-06-28T17:07:17.000Z
[thinking] I need to insert the new FAQ entry after line 534 and before line 536 (## 许可与致谢). The old string will be from the last A: line to the ## header.
--- edit({"filePath":"/mnt/nvme2t_2/easytier/README.md","oldString":"**Q: agent 无法同步白名单怎么办？**\nA: 检查 `WHITELIST_SYNC_URL` 是否正确、admin 是否可达。admin 失联时 agent 会使用最后一次同步的白名单，不会放行新设备。\n\n## 许可与致谢","newString":"**Q: agent 无法同步白名单怎么办？**\nA: 检查 `WHITELIST_SYNC_URL` 是否正确、admin 是否可达。admin 失联时 agent 会使用最后一次同步的白名单，不会放行新设备。\n\n**Q: 日志中反复出现 `[BIND_DEBUG] auto-bound hostname` 是什么意思？**\nA: 表示某白名单条目的 hostname 为空，核心自动将当前连接设备的主机名绑定到该 IP。如果 admin 自身 IP（如 10.0.10.1）每 30 秒出现此日志，说明 admin 启动时未成功写入自身白名单条目。请检查容器启动日志中是否有 SQLite 错误。\n\n## 许可与致谢"}) [completed]
Edit applied successfully.

## [assistant] +058391-06-28T18:08:28.000Z
[thinking] All 4 edits are complete. Let me update the todo list and verify the final result by reading sections of the file.
--- todowrite({"todos":[{"priority":"high","content":"Add admin auto-register bullet to 工作原理 section","status":"completed"},{"priority":"high","content":"Add Bug 7 (INSERT OR IGNORE) and Bug 8 (close_peer) to 修复的 Bug section","status":"completed"},{"priority":"high","content":"Add RUST_LOG row to Agent 环境变量 table","status":"completed"},{"priority":"medium","content":"Add FAQ entry for [BIND_DEBUG] auto-bound hostname","status":"completed"}]}) [completed]
[
  {
    "content": "Add admin auto-register bullet to 工作原理 section",
    "status": "completed",
    "priority": "high"
  },
  {
    "content": "Add Bug 7 (INSERT OR IGNORE) and Bug 8 (close_peer) to 修复的 Bug section",
    "status": "completed",
    "priority": "high"
  },
  {
    "content": "Add RUST_LOG row to Agent 环境变量 table",
    "status": "completed",
    "priority": "high"
  },
  {
    "content": "Add FAQ entry for [BIND_DEBUG] auto-bound hostname",
    "status": "completed",
    "priority": "medium"
  }
]

## [assistant] +058391-06-28T18:49:10.000Z
所有 4 处 README 修改已完成：

1. **工作原理** — 新增 bullet：admin entrypoint.sh 自动 INSERT OR IGNORE 自身 IP 到白名单
2. **修复的 Bug** — 新增 Bug #7（INSERT OR IGNORE 缺少 created_by/created_at）和 Bug #8（close_peer_conn 静默失败→改用 close_peer）
3. **Agent 环境变量** — 新增 `RUST_LOG` 行，默认值 `easytier=error`
4. **FAQ** — 新增 `[BIND_DEBUG] auto-bound hostname` 含义及排查方法
